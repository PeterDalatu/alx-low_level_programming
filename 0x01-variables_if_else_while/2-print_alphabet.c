#include <stdio.h>
#include <stdlib.h>
/**
 * main - Prints the alphabet
 * Return: Always (Succes)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
